CREATE SCHEMA IF NOT EXISTS raw;

CREATE TABLE IF NOT EXISTS raw.mqtt_sensor_event (
    raw_hash TEXT PRIMARY KEY,
    source_event_id BIGINT NOT NULL UNIQUE,
    event_time TIMESTAMPTZ NOT NULL,
    received_at TIMESTAMPTZ NOT NULL,
    loaded_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,
    broker_alias TEXT NOT NULL,
    topic TEXT NOT NULL,
    qos SMALLINT NOT NULL,
    retained BOOLEAN NOT NULL,
    classification TEXT NOT NULL,
    machine_id TEXT NOT NULL,
    machine_type TEXT NOT NULL,
    supplier TEXT,
    site_id TEXT,
    zone TEXT,
    room_id TEXT,
    batch_id TEXT,
    temperature_c NUMERIC(7, 3),
    humidity_pct NUMERIC(7, 3),
    pressure_bar NUMERIC(9, 3),
    vibration_mm_s NUMERIC(9, 3),
    motor_current_a NUMERIC(9, 3),
    rpm INTEGER,
    cycle_count BIGINT,
    state TEXT,
    alarm_code TEXT,
    payload_json JSONB NOT NULL
);

INSERT INTO raw.mqtt_sensor_event (
    raw_hash,
    source_event_id,
    event_time,
    received_at,
    broker_alias,
    topic,
    qos,
    retained,
    classification,
    machine_id,
    machine_type,
    supplier,
    site_id,
    zone,
    room_id,
    batch_id,
    temperature_c,
    humidity_pct,
    state,
    payload_json
)
VALUES (
    repeat('a', 64),
    -1000000000001,
    CURRENT_TIMESTAMP - INTERVAL '2 seconds',
    CURRENT_TIMESTAMP - INTERVAL '1 second',
    'mqtt-edge-042',
    'pharma/production/site-042/sensor/SENSOR_06',
    1,
    false,
    'production_machine_signal',
    'SENSOR_06',
    'sensor',
    'Endress+Hauser',
    '042',
    'Quai expédition froid',
    'QF-002',
    'LOT-CI-0001',
    5.2,
    52.0,
    'RUNNING',
    '{"machine_id":"SENSOR_06","machine_type":"sensor","temperature_c":5.2}'::jsonb
)
ON CONFLICT (raw_hash) DO NOTHING;
